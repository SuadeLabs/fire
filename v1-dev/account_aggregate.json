{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Aggregated Account Schema",
  "description": "An Aggregated Account represents a grouping of accounts into a single data object.",
  "type": "object",
  "allOf": [{
    "$ref": "https://github.com/SuadeLabs/fire/blob/master/v1-dev/account.json"
  }],
  "properties": {
    "count": {
      "description": "Describes the number of accounts aggregated into a single row.",
      "type": "integer",
      "minimum": 2
    },
    "balance_500keur": {
      "description": "Indicates that the balance of each account within the aggregate had a balance greater than 500k EUR.",
      "type": "boolean"
    }
  },
  "required": ["count"],
  "additionalProperties": true
}
